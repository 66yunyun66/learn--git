				工作区和暂存区
	1，工作区：我们创建的文件夹learn--git就是工作区
	2，暂存区：工作区中的.git文件夹是git的版本库，其中最重要的就是stage暂存区，还有
	git自动创建的第一个分支master，和指向master的指针head。
	一个工作文件夹应该就一个分支，这个分支记录了每次的修改。
	3，git add 把文件修改添加到暂存区，git commit将暂存区的所有内容提交到当前分支。
	提交后暂存区就会被清空。。。